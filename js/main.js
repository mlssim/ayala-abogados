/* ============================================
   AYALA ABOGADOS
   NOTA: este archivo se llamaba "main.js" pero contenía
   una copia casi íntegra de css/style.css guardada por error
   con extensión .js (ya venía así en el proyecto original).
   Como es "<script>" clásico, el navegador intentaba
   interpretar CSS como JavaScript y fallaba con un error de
   sintaxis en cada carga de página (visible en la consola).

   La lógica real de interfaz (menú móvil, modo oscuro,
   animaciones, scroll-to-top) ya vive en js/responsive.js,
   que se carga justo después de este archivo en todas las
   páginas. Se deja este archivo vacío (en vez de borrar la
   etiqueta <script> de 8 páginas) para no tocar más archivos
   de los necesarios.
   ============================================ */
